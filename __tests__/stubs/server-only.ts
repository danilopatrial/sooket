// Stub for 'server-only' in test environments. The real package throws
// at import time when running in a browser/client bundle; in tests we just
// allow the import to proceed without side effects.
export {};
